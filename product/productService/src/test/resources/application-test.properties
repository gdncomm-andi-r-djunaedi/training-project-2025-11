# Test Configuration
spring.application.name=productService-test
spring.data.mongodb.uri=mongodb://localhost:27017/product-service-test
server.port=0

# Disable Elasticsearch for integration tests (unless testing ES specifically)
spring.data.elasticsearch.repositories.enabled=false

# Disable caching in tests
spring.cache.type=none

# Disable data seeder in tests
spring.jpa.hibernate.ddl-auto=none

