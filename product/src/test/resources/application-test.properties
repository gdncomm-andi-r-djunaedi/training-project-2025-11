# Embedded MongoDB Configuration for Testing
# de.flapdoodle.embed.mongo auto-configures embedded MongoDB

# MongoDB settings (will be overridden by embedded MongoDB)
spring.data.mongodb.database=testdb

# Logging
logging.level.com.marketplace=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

