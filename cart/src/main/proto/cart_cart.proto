syntax = "proto3";

option java_multiple_files = true;
option java_package = "com.training.marketplace.cart.entity";

import "product_cart.proto";

// Represents a user's shopping cart.
message UserCart {
  string user_id = 1;
  repeated ProductCart products = 2;
}
